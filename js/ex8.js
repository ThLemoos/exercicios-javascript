// Exsercício com array

alert('Verificação de média');

function calculo() {
    let notas = [7, 8, 6, 9];
    const media = (notas[0] + notas[1] + notas[2] + notas[3]) / 4;
    return media;
}
alert(`O valor da sua média é: ${calculo()}`);

// Imprimir array no console
